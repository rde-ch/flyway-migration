spring:
    profile: green
    main:
      web-application-type: none

    jpa:
      hibernate:
        ddl-auto: none #IMPORTANT -> Headless

    flyway:
      enabled: true #running via CI
      #schemas: addressmig_green
      default-schema: addressmig_green
      baseline-on-migration: true
      locations: classpath:db/migration-addressmig_green



